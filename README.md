# README

Things you may want to cover:

* Ruby version - 3.3.9

* Database creation & initialization - config/database.yml

* Generate the Book model + migration, add the model code (validations + helper methods), and run the migration

* Add constraints and index - in migration file - db/migrate/TIMESTAMP_create_books.rb - Add the columns and fields to db

* Edit model file (app/models/book.rb) - equivalent to models.py

* define the API endpoints (config/routes.rb)

* Controller (app/controller/application_controller.rb) - handles http requests, talks to model and returns responses. Each endpoint maps to a controller method

* How to run the test suite

* Services (job queues, cache servers, search engines, etc.)

* Deployment instructions

* ...
